# _CHANGELOG.md

> 知識庫變更紀錄（最新在上，超過 8 筆觸發滾動淘汰）

| 日期 | 變更 | 影響文件 |
| 2026-04-26 | **fix(subagent-bridge): 完成通知改分段 reply（多訊息）+ 不再硬切 500 字** — (1) 完成通知不再硬切 500 字，改 1900 字 inline 或 .md 附件；(2) 進一步改為分段 reply（多訊息），不用附件 — 行動裝置可直接讀，無需開附件 | src/core/subagent-discord-bridge.ts |
| 2026-04-26 | **feat(dashboard-models): 模型快捷依憑證 provider 分組 + 動態抽取** — (1) 模型快捷依 auth profiles 的 provider 動態分組（跟著 auth profile 變動）；(2) 模型面板依 provider 列出 models.json 全部模型；(3) 內建模型清單改從 pi-ai 動態抽取（不再寫死） | src/core/dashboard.ts, models-config.json |
| 2026-04-26 | **feat(memory-vector): embedding 模型漂移偵測 + 完整重建模式** — (1) 偵測 namespace dim mismatch，dashboard 警示 banner 提示重建索引；(2) `upsert` 自動處理 dim mismatch — drop+rebuild 該 namespace；(3) 完整重建模式（drop + seed），`deleteAtom` 同步清向量 DB | src/vector/, src/memory/{engine,memory-api}.ts, src/core/dashboard.ts |
| 2026-04-26 | **feat(context-engine): CE 視野擴大 + Tool LRU Eviction** — 治本 tool_search 活化造成的 cache 累計成本；CE 觀察視野擴大，搭配 Tool 結果 LRU eviction 控制長期 turn 累積膨脹 | src/core/context-engine.ts |
| 2026-04-26 | **fix(provider/config): ollama 預設啟用 + quota error 觸發 failover + embedding init fail loud** — (1) `config.ts` ollama 預設 `enabled: true`，避免初次安裝後 ollama 未開即 startup fail；(2) provider quota error 觸發 failover；(3) embedding init 失敗改為 startup fail loud（不再 silent skip） | src/core/config.ts, src/providers/, src/vector/embedding-provider.ts |
| 2026-04-26 | **feat(trace): markdown 補 CE 前後 messages + Dashboard 批次操作** — (1) trace export markdown 補上 `messagesAfterCE` / `messagesBeforeCE`，可審計 compaction 摘要品質；(2) trace 列表加入批次選取 / 批次匯出 (.zip) / 批次刪除 | src/core/{dashboard,message-trace}.ts |
| 2026-04-25 | **maintenance: cli-bridge / clear-skill / cron-skill / token A/B** — (1) cli-bridge mid-stream split 後 result flush 去重，避免訊息重複送；(2) `/clear` 預設只清 session，`/clear all` 才連 trace 一起清；(3) cron-skill 補齊語法描述 + 區分 validation/error，省一輪 LLM call；(4) token A/B 優化（C 確認 pi-ai 已內建） | src/cli-bridge/reply.ts, src/skills/builtin/{clear,cron}.ts |
| 2026-04-24 | **feat(agent-loop): 主對話載入 agent skills + 散彈式重複守門 + CATCLAW 工具邊界** — (1) 主對話也載入 agent skills + 自建提示（原本只在 subagent 載入）；(2) 散彈式重複偵測 + 0-progress 守門，還原 playwright/computer-use deferred；(3) CATCLAW.md 加「對話 vs 任務的工具邊界」硬規則 — 當前頻道回覆直接寫文字，不用 MCP | src/core/agent-loop.ts, templates/CATCLAW.md, agents/wendy/CATCLAW.md |
| 2026-04-23 | **feat(agent-loop): 插話 / 訊息編輯改用 soft-inject，不打斷當前 turn** — 原本插話會 abort 當前 turn，造成 user prompt 與正在組裝的回覆雙雙丟失（user 看不到任何回應）。改為 soft-inject：當 turn 結束前注入 `[使用者新訊息（turn 進行中插入）]`，模型自行評估是否轉向；abort 路徑保留但不丟 user prompt 與已生成內容 | src/core/agent-loop.ts |
| 2026-04-23 | **fix(agent-loop): deferred tool 完整修補七連發** — (1) 活化改信任 tool_search result，禁模糊匹配；(2) 解決 tool_search 後空回應死循環（複合根因修法）；(3) 空回應 end_turn 自動續接 + 強化 deferred 指示；(4) deferred tool 活化節流，避免一次活化多個觸發 Anthropic 空回應；(5) eager 寫死 + provider error 不再被吞；(6) deferred nudge 連續重試失效修復 + trace turnIndex 與 tool-log 檔名差 1 修復；(7) deferred nudge 上限 1 → 3 + 耗盡後給使用者通知；(8) bail notice 改 yield text_delta（原本改 done.text 沒進 Discord） | src/core/agent-loop.ts |
| 2026-04-23 | **fix(codex-oauth): 共用 ~/.codex/auth.json + nested 格式 + JWT exp** — (1) catclaw 跟 Codex CLI 共用 `~/.codex/auth.json`，避免雙方 refresh 互踩；(2) 支援 Codex CLI nested auth.json 格式 + JWT exp 解析；(3) cli-bridge codex approval decision enum 採用新 API + 繼承全域 ~/.codex 設定；(4) codex sandboxPolicy.networkAccess 型別應為 boolean | src/providers/codex-oauth.ts, src/cli-bridge/providers/ |
| 2026-04-23 | **fix(mcp/discord): multipart 上傳強化** — 顯式 mime type + Headers 物件 + 失敗診斷 log；mcp 四處 LLM 錯誤輸入耐受性修正 | src/mcp/discord-server.ts |
| 2026-04-23 | **maintenance: dashboard 顯示與互動修補** — (1) `/api/tool-log` session/turn 永遠讀不到修復；(2) tool 點擊展開完整內容 + 放寬 MCP timeout 與 base loop；(3) `computeBuildInfo` 在 Windows 上 git 指令拿不到 commit 修復；(4) dashboard 版本顯示 + agent-loop 自適應 loop cap；(5) `clearLogs()` confirm 字串 literal newline → JS SyntaxError 修復；(6) cli-bridge 頻道名循環剝除歷史尾綴，防止 `_catclaw_catclaw_catclaw` 疊加 | src/core/{dashboard,agent-loop}.ts, src/cli-bridge/channel-naming.ts |
| 2026-04-27 | **fix(agent-loop): 強化 soft-inject framing，防止插話被誤讀為 end_turn 線索** — 原本 `[使用者插話] {msg}` 對模型語意過弱，模型常將其當「補充 context」而非「new instruction」，注入後仍直接 `end_turn` 結束 turn（trace 2aa73911 實例：使用者插話「HR系統」後 Loop #8 stop=end_turn，turn 提早結束、未轉向處理）。改為 `[使用者新訊息（turn 進行中插入）] {msg}` + 明確指示「當作 user 新請求對待，重新評估方向，除非任務真正完成否則不要 end_turn」 | src/core/agent-loop.ts |
| 2026-04-27 | **feat(health-monitor): Component-level fail-loud + 通報機制（反靜默失敗）** — 解決「Ollama model name 對不上實際安裝（catclaw.json 設 `qwen3:14b` 但本機只有 `qwen3:1.7b`、設 `qwen3-embedding:8b` 但本機只有 `qwen3-embedding:latest`），但啟動時只印『初始化 provider』看似 ok，後續 graceful skip 一直 silent fail，12 天無人發現，memory-extractor 22 次 flush 無一次成功萃取」此類 silent failure 問題。三層實作：(L1) **Fail-loud + Startup Health Summary** — `OllamaClient.verifyModel/verifyAllModels()` 用 POST /api/show 驗 model 存在；`Embedding/ExtractionProvider` 介面新增 `verify?()`；`platform.ts` 在 `_ready=true` 之前 `runStartupHealthCheck()` 集中印紅綠燈摘要。(L2) **Component health tracker** — 新增 `core/health-monitor.ts`，門檻 degraded=2 / critical=5 連續失敗，emit `health:degraded`/`critical`/`recovered`，critical 1 小時節流；`OllamaEmbeddingProvider.embed()`、`GoogleEmbeddingProvider.embed()`、`OllamaExtractionProvider.generate/chat()` 所有 graceful skip 點接 record。(L3) **Dashboard + Discord 通報** — 新增 `GET /api/health` endpoint；日誌頁加「🩺 Component Health」面板（紅綠燈總覽 + 表格 + startup details）；`index.ts` 訂閱 health:* event → Discord errorNotifyChannel | src/core/{health-monitor.ts(NEW),event-bus,platform,dashboard}.ts, src/ollama/client.ts, src/vector/embedding-provider.ts, src/memory/extraction-provider.ts, src/index.ts, _AIDocs/modules/health-monitor.md(NEW), _AIDocs/_INDEX.md |
| 2026-04-23 | **feat: /capabilities skill + Obsidian 同步 hooks + 跨頻道 mention 路由修正** — (1) 新增 `/capabilities`（`/caps`）skill，列出 CatClaw 平台全部可用能力（hooks/tools/skills/modules/mcp），tier=public，所有 agent 皆可查詢；(2) CatClaw 側新增 `PostAtomWrite.obsidian-export.ts` hook，atom 寫入後自動同步到 Obsidian 知識庫（`~/WellsDB/知識庫/CatClaw/{agent}/`），vault_ 前綴防迴圈；Claude Code 側新增 `obsidian-sync.py`（Python 跨平台），Write/Edit memory 時同步到 Obsidian（`~/WellsDB/知識庫/ClaudeCode/{slug}/`）；(3) 跨頻道 mention 路由重構：優先路由到 suspended bridge → 無 suspended 則 auto-spawn 新 bridge（模板用第一個 config，requireMention=true，持久化到 cli-bridges.json）→ 完全無 bridge 則 fallback agent loop | src/skills/builtin/capabilities.ts, src/cli-bridge/index.ts, ~/.catclaw/workspace/hooks/PostAtomWrite.obsidian-export.ts, ~/.claude/hooks/obsidian-sync.py, _AIDocs/guides/obsidian-sync-setup.md |
| 2026-04-23 | **feat(dashboard): MCP 一鍵預設新增 unity-mcp** — Dashboard Config 頁 `_mcpPresets` 加入 `unity-mcp`（CoplayDev/unity-mcp v9.6.x，stdio transport）。啟動指令：`uvx --from mcpforunityserver mcp-for-unity --transport stdio`；tier=elevated。前置條件：(1) 本機裝 `uv`（`brew install uv`）；(2) Unity 專案裝 UPM package `https://github.com/CoplayDev/unity-mcp.git?path=/MCPForUnity#main` 並 `Window > MCP for Unity > Start Server`（HTTP 8080） | src/core/dashboard.ts, _AIDocs/modules/dashboard.md |
| 2026-04-23 | **chore(gitignore): 忽略 `.claude/memory/feedback_*.md`** — 新增 feedback atom 由 Claude 自動萃取，不應進入 repo | .gitignore |
| 2026-04-23 | **feat: 時間脈絡強化（避免 LLM 以 cutoff 推斷錯誤年份）** — (1) `prompt-assembler.ts` date-time 模組從簡短「當前時間」改為「系統時鐘」格式，注入今日完整日期（年月日+星期）+ 當前時間，並明示「使用者說『今天』『昨天』『這週』等相對時間時以此日期為基準」；(2) `cli-bridge/bridge.ts` inbound 訊息 `<channel>` tag 加 `current_time="YYYY-MM-DDTHH:mm:ss+08:00"` 屬性（Asia/Taipei），上線補處理與 bridge 內注入的所有訊息都帶時間戳 | src/core/prompt-assembler.ts, src/cli-bridge/bridge.ts, _AIDocs/modules/prompt-assembler.md |
| 2026-04-23 | **feat: Discord messageUpdate 處理（編輯訊息感知）** — (1) 新增 `messageUpdate` event listener（支援 partial fetch）；(2) Debounce 窗口內：`debounceMessageIndex` Map 追蹤 message ID → buffer 位置，編輯時直接替換 buffer 內容，agent 拿到修正後版本；(3) 已 dispatch 後：偵測 channel 是否有 active CLI Bridge（status=busy），透過 `bridge.send()` 注入 `[訊息編輯]` 通知，Claude Code 收到後自行調整回覆方向（利用既有插話機制中斷舊 turn） | src/discord.ts |
| 2026-04-22 | **fix(dashboard): Inbound History 展開無 entries** — (1) `listChannels()` regex 從 `^discord_(.+?)` 改為 `^discord_(\\d+?)(?:_([a-zA-Z][^.]*))?` 修正 channelId/scope 分割錯位；(2) Dashboard API handler `url.match(/[?&]channelId=/)` 永遠找不到（`url` 是 pathname 不含 query string），改用 `urlParams.get("channelId")` | src/discord/inbound-history.ts, src/core/dashboard.ts |
| 2026-04-22 | **fix(build): Playwright MCP Windows 建置失敗** — build script 加 `pnpm -r install` 確保子 workspace packages 的 node_modules 存在 | package.json |
| 2026-04-22 | **feat: ExtractionProvider 記憶萃取抽象層** — 記憶系統脫離 Ollama 依賴，新增 `ExtractionProvider` interface 支援 Ollama/Anthropic/OpenAI 三 provider。auto-resolve Anthropic API key from auth-profile.json。setup.sh/ps1 新增 Ollama 偵測 + 無 Ollama 自動切 Anthropic provider | src/memory/extraction-provider.ts, src/memory/{extract,session-memory}.ts, src/core/{platform,config}.ts, setup.sh, setup.ps1 |
| 2026-04-21 | **feat(setup): MCP Servers 互動設定步驟** — setup.sh + setup.ps1 功能開關步驟新增 MCP server 選擇：Discord MCP（預設 Y）、Computer Use MCP（y/N，含截圖寬度/允許視窗/歷程目錄細節設定）、Playwright MCP（y/N，含瀏覽器/viewport/headless 細節設定）。選 Y 自動寫入 mcpServers 設定，選 N 提示可從 Dashboard 一鍵新增 | setup.sh, setup.ps1 |
| 2026-04-21 | **feat(dashboard): MCP Servers 模組化設定 — env 欄位 + 一鍵預設快速新增** — Dashboard Config 分頁 MCP Servers 區塊新增：(1) `env` 欄位（map 類型 key-value 編輯器）用於設定 MCP server 環境變數；(2) `_mcpPresets` 預設快速新增按鈕（Discord / Computer Use / Playwright），一鍵插入預填 command/args/env/tier，已存在的 preset 自動隱藏按鈕 | src/core/dashboard.ts, _AIDocs/modules/dashboard.md |
| 2026-04-21 | **feat(playwright-mcp): 整合 Playwright MCP 到 CatClaw workspace** — 新增 `mcp/playwright/` 獨立 MCP Server，封裝 `@playwright/mcp` 提供 headless 瀏覽器自動化（預設 headless + chromium + 1280x720）。環境變數控制：`PLAYWRIGHT_HEADLESS`/`PLAYWRIGHT_BROWSER`/`PLAYWRIGHT_VIEWPORT`/`PLAYWRIGHT_USER_DATA`/`PLAYWRIGHT_TIMEOUT`/`PLAYWRIGHT_NAV_TIMEOUT`。pnpm workspace + build script 整合 | mcp/playwright/（src/index.ts, package.json, tsconfig.json）, pnpm-workspace.yaml, package.json |
| 2026-04-21 | **fix(computer-use): 座標等比縮放 + 拖曳插值 + BGRA 色版 + tool description 修正** — (1) `image.ts` 截圖預設長邊改 1024（`COMPUTER_USE_MAX_SCREENSHOT_WIDTH` env 覆寫），移除 `MAX_SHORT_SIDE` 雙重限制，`fit: "fill"` → `fit: "inside"` 等比縮放；(2) 新增 `coordinate.ts`（`screenshotToScreen` / `updateScreenshotScale`），click/cursor/scroll 三工具自動換算座標；(3) `cursor.ts` drag 操作改為 10px 步進插值滑動（避免即時傳送導致遊戲不認拖曳）；(4) `screenshot.ts` Windows 平台 BGRA→RGBA channel swap；(5) 所有座標工具 description 標註「座標使用截圖空間，系統自動換算」避免 AI 重複換算 | mcp/computer-use/src/{utils/{image,coordinate}.ts, tools/{screenshot,click,cursor,scroll}.ts, index.ts} |
| 2026-04-21 | **fix: discordUpload 雙重 drive letter 路徑修正** — AI 產生 `C:\C:\Users\...` 路徑時 `readFileSync` 失敗，加 regex 去除重複 drive letter 前綴 | src/mcp/discord-server.ts |
| 2026-04-21 | **docs: catclaw.example.json 加入 playwright MCP 範例 + screenshot 預設改 1024** | catclaw.example.json |
| 2026-04-21 | **fix: 工具逾時/失敗時立即推送 Discord 通知** — 原本 tool error（如 run_command 逾時）靜默回給 LLM，使用者看不到即時通知。reply-handler.ts 新增 `tool_result` error 處理（❌ 通知）；cli-bridge/reply.ts tool_result 失敗顯示 ❌（不論 showToolCalls 設定）；cli-bridge/process.ts + types.ts tool_use_summary 解析加入 error 欄位 | src/core/reply-handler.ts, src/cli-bridge/{reply,process,types}.ts, _AIDocs/modules/reply.md |
| 2026-04-21 | **feat: CLI Bridge 上線後自動補處理 inbound history** — 原本 `start()` 只 fire `sendStartupNotification`（「已上線」通知），離線期間累積到 inbound history 的訊息要等下一則新訊息進來才會被 `consumeBridgeInboundHistory` 消費。新增 `drainInboundHistoryOnStartup()` 排在 sendStartupNotification 後 fire-and-forget：`consumeForInjection(channelId, ..., "bridge:{label}")` 拿 bridge scope 全部 entries → 加前綴後 `bridge.send()` 丟進 CLI 自動處理。無 entries 時 noop；消費後 JSONL 自動清空避免重複處理 | src/cli-bridge/bridge.ts, _AIDocs/modules/cli-bridge.md |
| 2026-04-21 | **feat: 權限請求逾時預設 60s → 10 分鐘 + env 覆寫** — CLI Bridge 兩條權限審批路徑（`cli-bridge/reply.ts` control_request fallback + `mcp/discord-server.ts` D3 主路徑）原本硬編 60 秒逾時，對話稍微延遲或 agent 並行思考時就會被自動拒絕。改為預設 600_000ms（10 分鐘），並接受環境變數 `CATCLAW_PERMISSION_TIMEOUT_MS` 覆寫。訊息文字的 `60s` 提示改為動態從常數計算 | src/mcp/discord-server.ts, src/cli-bridge/reply.ts, _AIDocs/modules/cli-bridge.md, _AIDocs/02-CONFIG-REFERENCE.md |
| 2026-04-21 | **fix: Tool Loop 4b 寬鬆防線誤擋探索性呼叫** — 舊邏輯：`run_command` 最近 10 次中 ≥8 次就擋（只看 tool name count），導致 agent 做路徑探索（不同 ls/find/pwd/where）也被當成無效重試。新邏輯疊加兩個條件（A+B 交集）：(A) args 相似度（`run_command` 比對 command 前 30 字、file_* 比對 path、其他 fallback JSON 前 60 字）(B) 結果失敗（`error` 存在 / `exitCode ≠ 0` / `isError`）。僅當 8+ 同名呼叫中 ≥5 次同時符合「失敗 + args 相似於當前 call」才判定為無效重試。不會誤擋探索性差異化呼叫，只攔真正的鬼打牆 | src/core/agent-loop.ts, _AIDocs/modules/agent-loop.md |
| 2026-04-21 | **fix: Dashboard 儲存後寫出 safety.enabled=false 導致下次啟動失敗（S17）+ discord.js v14 deprecation** — (1) `config.ts` `safety` / `workflow` 區塊由 `raw.x ? {...} : undefined` 改為永遠展開（預設值內嵌），避免 `raw.safety` 缺失時 resolved config 為 undefined → Dashboard GUI bool 欄位渲染為 unchecked → `collectConfigJSON` 寫出 `enabled: false` → 下次啟動被 S17 拒絕；`safety` 同步補上原本遺漏的 `collabConflict` / `reversibility` 型別欄位傳遞；(2) `dashboard.ts` POST `/api/config` 寫入前守門：`safety.enabled === false` 或 `safety.selfProtect === false` 直接 400 拒絕，避免寫入損壞的設定檔；(3) `cli-bridge/discord-sender.ts` SharedBotPool 兩處 `client.once("ready")` 改用 `Events.ClientReady`（v14 deprecation：`ready` v15 將只發 gateway READY，改用 `clientReady`） | src/core/{config,dashboard}.ts, src/cli-bridge/discord-sender.ts |
| 2026-04-20 | **docs: _AIDocs 審計修正** — _INDEX.md / WIKI.md 更新日期、02-CONFIG-REFERENCE.md 補 computer-use MCP server 範例 | _INDEX.md, WIKI.md, 02-CONFIG-REFERENCE.md |
| 2026-04-20 | **feat: Computer Use MCP Server（Phase 1-4）+ rich content pipeline** — (1) 新增獨立 MCP Server `mcp/computer-use/`（9 tools：screenshot/click/type/scroll/cursor/windows/screen-info/history/test）、測試框架（task-template/task-runner/report）、2 模板（TSLG smoke/UI flow）、安全機制（白名單/速率限制/座標邊界）、操作歷程 + 卡住偵測；(2) `ToolResult` 新增 `contentBlocks` 欄位支援 rich content；(3) `ToolResultBlock.content` 擴展為 `string \| Array`；(4) `McpClient` 新增 `callRich()` 支援 image content blocks；(5) agent-loop pipeline 偵測 contentBlocks 直通 rich content；(6) 5 個 provider 適配：claude-api（image→Vision）、ollama/openai-compat/codex-oauth/acp-cli（fallback JSON）；(7) catclaw.json 新增 computer-use MCP server 設定（tier: elevated） | mcp/computer-use/（16 files）, src/tools/types.ts, src/providers/base.ts, src/mcp/client.ts, src/core/{agent-loop,context-engine}.ts, src/providers/{claude-api,ollama,openai-compat,codex-oauth,acp-cli}.ts, catclaw.json |
| 2026-04-19 | **fix: 圖片 media_type magic bytes 偵測 + Dashboard trace 錯誤標記** — (1) reply.ts `extractAttachments` 下載圖片後用 magic bytes 偵測真實格式（PNG/JPEG/GIF/WebP），不信任 Discord contentType（Discord 常報 webp 但實為 png → API 400）；(2) Dashboard trace overview 新增 Err 欄位，掃描 `llmCalls[].toolCalls[].error` + 頂層 `error`/`status=error`，有錯誤時紅色 badge `⚠ N` + hover 詳情 + 淡紅底色 | src/cli-bridge/reply.ts, src/core/dashboard.ts, _AIDocs/modules/dashboard.md |
| 2026-04-19 | **feat: Dashboard Trace 匯出 Markdown + CliBridge 刪除按鈕 + Trace ID 雙擊複製** — (1) Trace Detail 標題 Trace ID 加 `ondblclick` 雙擊複製到剪貼簿（複製成功顯示 ✅ copied! 1s 後還原）、旁邊新增「📥 匯出」按鈕連到 `/api/traces/:id/export`；(2) 新增 `GET /api/traces/:traceId/export` endpoint，回傳整筆 trace 的 Markdown（含基本欄位表、Inbound、Context Assembly、LLM Call Loop 每 iteration 的 tool calls 與 tokens）；(3) CLI Bridge detail 頁面新增「🗑 移除」按鈕 + `cbDeleteBridge()` 透過 `DELETE /api/cli-bridge/:label` 關閉 process + 從 cli-bridges.json 移除設定 | src/core/dashboard.ts |
| 2026-04-19 | **fix: bot-token 共用雙路由修正（discord.ts + slash.ts）** — 主 bot Client 與 CliBridge SharedBotPool 共用同 token 時，兩個 Client 都會收到 messageCreate / interactionCreate，造成訊息/指令被雙重處理。(1) `discord.ts` CLI Bridge 路由區塊 independent-bot 分支原本 log 後 fall through 到 AgentLoop，導致無記憶的 AgentLoop 搶先回覆蓋掉 CliBridge 的上下文（user 回報「agent 不知道『好』什麼」的失憶症狀），改為 `return`，頻道一旦綁定 CliBridge，主 bot AgentLoop 就不在該頻道執行；(2) `slash.ts` 新增 `_handledInteractions` 去重 set（30s TTL），避免多 Client 重複執行同一 slash command；(3) interaction error reply 本身也包 try-catch，避免回覆失敗造成第二次錯誤 | src/{discord,slash}.ts, _AIDocs/modules/discord.md |
| 2026-04-19 | **feat: restart recovery + skill admin 權限鏈修正 + reply fallback** — (1) TaskStore 磁碟持久化（initTaskPersistence + loadFromDisk + loadAllPersistedTasks）；(2) 重啟上線通知（discord.ts sendRestartNotification + bridge.ts sendStartupNotification，只通知 restartNotify.channels 明確設定的頻道）；(3) skill tool 權限鏈修正（admin agent 跳過 checkTier + effectiveAuthorId 用 allowedUserIds[0]）；(4) agent-loop ToolContext 三處補上 isAdmin 傳遞；(5) reply.ts result fallback：buffer 為空時送 result.text，is_error 無文字送提示（修正 permission deny 後無回應） | src/{core/{task-store,agent-loop,config,platform},cli-bridge/{bridge,reply},discord,tools/builtin/{skill,config-patch},skills/builtin/config-manage,templates/CATCLAW}.{ts,md}, catclaw.example.json, _AIDocs/modules/{task-store,config,platform,discord,tool-registry,cli-bridge}.md |
| 2026-04-18 | **docs: 全專案文件審計修正** — 4 agent 平行掃描後修正：(1) 02-CONFIG-REFERENCE 全面重寫（V2 三層分離、缺漏 config 區塊、預設值修正）；(2) README 中英版修正（hook 34→36、角色名稱、CE 5 策略、EN 目錄同步）；(3) wiki/Tools-and-Skills 補齊 9 tool + 15 skill；(4) PLAN 文件搬離 _AIDocs；(5) 00-OVERVIEW dashboard port 8088 + config 欄位表 + providers 檔案列表；(6) 01-ARCHITECTURE tool 25 個 + 記憶 4 層 + cli-bridge 目錄；(7) wiki 7 頁修正（Agent-Loop timeout、Session DM key、Provider V2、Message-Pipeline 13 模組、Discord circuit-breaker + inbound-history、Accounts Platform type）；(8) modules 4 檔修正（agent-loop 行數+事件表、config exports、memory-engine budget、skills 數量）；(9) PITFALLS 22 項 + legacy 標注；(10) _INDEX 策略名 + 事件數 + skill 數 | 02-CONFIG-REFERENCE.md, README.md, README.en.md, wiki/*.md, 00-OVERVIEW.md, 01-ARCHITECTURE.md, 09-PITFALLS.md, _INDEX.md, modules/{agent-loop,config,memory-engine,skills}.md, PLAN files → memory/_staging/ |
| 2026-04-18 | **docs: CE 完整規則同步** — wiki/Context-Engine.md 全文重寫對齊三段策略現況（原本仍描述已移除的 Budget Guard / Sliding Window），補上 Decay levels + 外部化 + Compaction 六段結構 + 意圖錨點 + 切割上限 + Anti-Hallucination + Hook 整合 + 設定 schema + 演進歷史。WIKI.md / _INDEX.md / 00-OVERVIEW.md / 01-ARCHITECTURE.md / modules/platform.md / wiki/Home.md 同步改掉 budget-guard / sliding-window 過時描述 | wiki/{Context-Engine,Home}.md, _AIDocs/{WIKI,_INDEX,00-OVERVIEW,01-ARCHITECTURE}.md, _AIDocs/modules/platform.md |
| 2026-04-18 | **feat: CE Compaction 摘要品質強化（A+B+C'）** — (A) compaction prompt 重寫為「agent 接續視角」第一人稱，system prompt 定位為紀錄員，user prompt 強制六段結構化章節（使用者意圖 / 已決策 / 待辦進行中 / 未解決問題 / 工具產出重點 / 重要事實限制），缺項以「無」填、不得省略；(B) 扁平化切割上限提高（SLICE_USER 500→2000、SLICE_ASSISTANT 500→1500、SLICE_TOOL_USE 120→500、SLICE_TOOL_RESULT 200→800）；(C') 摘要後追加「📌 使用者最近一則完整指令（原文，未壓縮）」錨點區塊（反向找 ≥50 char 的 user 訊息、取前 800 char，無額外 LLM call），防止摘要偏離意圖時後續 turn 接續偏航 | src/core/context-engine.ts, _AIDocs/modules/context-engine.md |
| 2026-04-18 | **fix: CE 外部化 + tool-log 路徑改為絕對路徑** — `externalizeMessage` / `ToolLogStore.save` 回傳值從相對路徑改為絕對路徑，修正 `read_file` 的 `resolve()` 以 `process.cwd()` 為 base 導致解析到專案目錄而非 CATCLAW_WORKSPACE/data 的問題。`buildExternalizedIndex` regex 同步更新匹配絕對路徑 | src/core/{context-engine,tool-log-store,prompt-assembler}.ts, _AIDocs/modules/{context-engine,tool-log-store,prompt-assembler}.md |
| 2026-04-17 | **feat: Stub 誠實化 + Anti-Hallucination 防線** — (1) `tool-log-store.ts` `buildIndexSummary` 格式升級為 `[工具索引 turn N]` + ⚠️ 警語；(2) `context-engine.ts` stub 改為 `[已壓縮 role turn N｜內容不可恢復，勿引用]`、外部化 stub 加「勿腦補」警語、compaction 過濾 stub/索引不做二次摘要、摘要加 `[對話摘要｜多輪壓縮]` 警語、`preserveRecentTurns` 5→8；(3) `prompt-assembler.ts` 新增 `context-integrity` module（priority 15）含鐵則 + retry escalation 防線，所有 intent 啟用；(4) `agent-loop.ts` turn cap warning（turnCount≥100 每 20 輪 warn）；(5) `config.ts` `ContextEngineeringConfig.turnCapWarning` 欄位 | src/core/{tool-log-store,context-engine,prompt-assembler,agent-loop,config}.ts, _AIDocs/modules/{tool-log-store,context-engine,prompt-assembler,agent-loop,config}.md, WIKI/{Context-Engine,Agent-Loop}.md, README.md, README.en.md |
| 2026-04-17 | **feat: Tool 截斷顯式化 + grep/glob 分頁 + atom 過肥警告** — (1) `agent-loop.ts` 4 個截斷策略（default/read_file/search/run_command）提示文字升級為 `[⚠️ CatClaw 已截斷：...]` + 明確告訴 agent 如何取回原文（offset/limit、更精確 pattern、寫檔後分頁）；(2) `grep` 新增 `offset` + `head_limit` 分頁參數，回傳結構新增 `offset` / `returned` / `hasMore` / `nextOffset` / `scannedCapHit` / `hint`；(3) `glob` 新增 `offset` + `limit` 分頁參數，回傳從純陣列改為 `{ paths, offset, returned, total, hasMore, nextOffset, scannedCapHit, hint }` 物件（minor breaking change）；(4) `context-builder.ts` atom 單顆 >budget 時 `log.warn` 建議拆分 | src/core/agent-loop.ts, src/memory/context-builder.ts, src/tools/builtin/{grep,glob}.ts, _AIDocs/modules/{agent-loop,memory-engine,tool-registry}.md, wiki/Tools-and-Skills.md |
| 2026-04-17 | **fix: Tool 執行錯誤回報三處修補** — (1) `spawn-subagent` timeout/failure 改設頂層 `ToolResult.error`（原本塞進 `result` 內層導致 `is_error=false`，agent 看不到失敗），attachment/worktree 失敗收集為 `warnings[]` 附掛結果；(2) `agent-loop.ts` Tool 執行後 `error` 路徑與 `result` 路徑拆開，**error 訊息永遠不過 truncate**（避免截斷後看不到原因）；(3) `registry.ts` + `platform.ts` `defaultTimeoutMs` 30000→0（不逾時），新增 `SOFT_WARN_MS=60_000` 軟警告（log.warn 不中斷），per-tool `timeoutMs` 覆寫仍生效 | src/tools/builtin/spawn-subagent.ts, src/core/{agent-loop,context-engine,platform}.ts, src/tools/registry.ts, _AIDocs/modules/{agent-loop,context-engine,tool-registry}.md, wiki/{Tools-and-Skills,Agent-Loop}.md |
| 2026-04-16 | **fix: FileWatcher hook 三處遺漏修正** — (1) `metadata-parser.ts` `VALID_EVENTS` 白名單補入 `FileChanged` / `FileDeleted`（scanner 跳過未知 event）；(2) `config.ts` resolved config 缺少 `fileWatcher: raw.fileWatcher` pass-through（Dashboard API 回傳空）；(3) `filewatch` / `hook_register` tool description 補入 event 名稱（agent 無法認知新 event） | src/hooks/metadata-parser.ts, src/core/config.ts, src/tools/builtin/{filewatch,hook-register}.ts, _AIDocs/modules/hooks.md |
| 2026-04-16 | **feat: Dashboard FileWatcher config GUI** — Config 分頁新增 File Watcher 區塊：enabled 開關、maxEventsPerWindow/eventWindowMs 全域限速、watches array 動態管理（label/path/ignoreDirs/ignorePatterns/debounceMs/cooldownMs/recursive）。新增 `renderArrayDynamic` + `addArrayDynEntry` + `removeArrayDynEntry` 支援 array-of-objects 類型設定（有別於既有 map 類型的 `renderDynamic`），`collectConfigJSON` 新增 array dynamic 收集邏輯 | src/core/dashboard.ts, _AIDocs/modules/dashboard.md |
| 2026-04-16 | **feat: FileWatcher Hook — 通用外部檔案監聽 + filewatch tool** — 新增 `FileChanged` / `FileDeleted` 兩個 hook event（34 events），`FileWatcher` class 含 5 層迴圈防護（suppressPath API / Hook Execution Context / per-path cooldown / global rate limit / CATCLAW_HOOK_DEPTH）。config.ts 新增 `FileWatcherConfig` + `fileWatcher?` 欄位。bootstrap.ts step 11 接線。新增 `filewatch` tool（list/add/remove 動態管理）。附 Obsidian 同步範例 hook template | src/hooks/{types,index,hook-registry,file-watcher}.ts, src/core/{config,platform}.ts, src/workflow/bootstrap.ts, src/tools/builtin/filewatch.ts, templates/hooks/FileChanged.obsidian-sync.ts, _AIDocs/modules/{hooks,tool-registry}.md |
| 2026-04-15 | **refactor: 移除 MCP Discord 頻道白名單** — `DISCORD_ALLOWED_CHANNELS` 環境變數移除，MCP Discord tool 不再限制只能操作綁定頻道，存取範圍改由 bot token 的 Discord 權限決定。配合跨頻道 mention 功能，讓 CLI session 能讀取/操作任意可見頻道 | src/cli-bridge/process.ts, src/mcp/discord-server.ts, _AIDocs/modules/cli-bridge.md |
| 2026-04-15 | **feat: CLI Bridge 跨頻道 mention 回應** — CLI Bridge bot 被 mention 時不再限於綁定頻道，可在任意頻道/thread/guild 回應。`BridgeSender.withChannel()` 建立 proxy sender 指向來源頻道；stdin channel tag 跨頻道時 `chat_id` 改為來源頻道 + `home_channel` 標記綁定頻道 + hint 提示跨頻道操作方式 | src/cli-bridge/{discord-sender,index,reply,bridge}.ts, _AIDocs/modules/cli-bridge.md |
| 2026-04-15 | **feat: Hook 系統全面擴展 — 32 events + defineHook SDK + 檔案掛載 + 3 管理工具 + /hook skill** — HookEvent 從 4 擴展為 32（9 類）；hooks 資料夾掃描（`workspace/hooks/` global + `agents/{id}/hooks/` per-agent）+ fs.watch 熱重載；支援 TS/JS/mjs/sh/bat/ps1 多 runtime；defineHook SDK 型別安全；32 觸發點全接線；新增 `hook_register`/`hook_list`/`hook_remove` + `/hook` skill + 3 templates。安全：CATCLAW_HOOK_DEPTH 防遞迴、fail-open、toolFilter | src/hooks/*, src/tools/builtin/hook-*.ts, src/skills/builtin/hook.ts, templates/hooks/*, _AIDocs/modules/{hooks,tool-registry,skills,agent-loop,platform,config,context-engine,memory-engine,providers}.md |
<!-- 滾動淘汰：refactor Agent 目錄合併至 workspace (2026-04-14)、feat 全域記憶寫入權限 globalMemoryWrite (2026-04-14)、feat Agent CATCLAW.md 統一載入 (2026-04-14)、feat skill tool (2026-04-14)、fix CLI Bridge quote 模式中間文字累積 (2026-04-14)、feat atom_delete tool (2026-04-14)、/cron skill + agentId 隔離 (2026-04-14)、docs README/WIKI/setup.sh (2026-04-13)、audit 100% 覆蓋率 (2026-04-13)、audit 知識庫全面同步 (2026-04-13)、fix CLI Bridge typing 不停止 (2026-04-12)、feat CE Externalization Decay (2026-04-12)、外部 bot mention 過濾 + CLI Bridge 中間推理文字格式化 (2026-04-12)、Wendy 資料路徑統一至 agents/wendy (2026-04-12)、Guard 分級 + Boot Agent ID (2026-04-12)、CLI Bridge idle suspend + SharedBotPool + /help (2026-04-12)、CE 漸進式衰減補完 (2026-04-12)、CE 漸進式衰減 Progressive Decay (2026-04-11)、CLI Bridge 雙重啟 race fix (2026-04-11)、CLI Bridge typing 不停止 turnDone+maxTypingTimer (2026-04-11)、移除 ModePreset.compaction 欄位 (2026-04-11)、CLI Bridge session 恢復改用 --resume (2026-04-11)、CLI Bridge session 保活 + graceful close + 插話模式預設 (2026-04-10)、session context 查詢 (2026-04-10)、/cd slash command + 獨立 bot slash 註冊 (2026-04-10)、Tool Summary 冷啟動注入 (2026-04-10)、Dashboard Trace agentId (2026-04-09)、memory write_file 向量同步 (2026-04-09)、AI 自建 Skill Sprint 5 (2026-04-09)、Agent Persona Skills Sprint 4 (2026-04-09)、Agent Recall Sprint 3 (2026-04-09)、Agent 記憶三層隔離 (2026-04-09)、persona → agent 命名統一 (2026-04-09)、Agent Persona System Sprint 1 (2026-04-09)、Dashboard Pipeline SyntaxError + Extract Model 選擇器 (2026-04-08)、File Size Guard + Failure Recall (2026-04-08)、Dashboard Memory tab SyntaxError + Extract 管線修復 (2026-04-08)、recall 品質修復 + Memory Dashboard (2026-04-08)、webchat 中文 IME Enter 選字誤觸送出 (2026-04-07)、CLI provider stdin + Codex OAuth 解析修正 (2026-04-07)、dashboard matchedBy + consolidate ACT-R 統一 (2026-04-07)、recall 管線三缺口修補 (2026-04-07)、recall 管線升級 Vector-Only 5→7 步 (2026-04-07)、Round 3 校閱修正 WIKI/README/PITFALLS (2026-04-07)、_AIDocs Round 3 審計 77 項修正 (2026-04-07)、記憶系統重建 Phase 2 Atom 清洗 (2026-04-07)、_AIDocs Round 16-20 審計 11 項修正 (2026-04-06)、_AIDocs Round 1-5 審計 8 項修正 (2026-04-06)、_AIDocs Round 11-15 審計 10 項修正 (2026-04-06)、_AIDocs 200 題交叉審計 51 項修正 (2026-04-06)、_AIDocs 審計 Session D (2026-04-06)、統一訊息管線 message-pipeline (2026-04-06)、_AIDocs 審計 Session C (2026-04-06)、Context Snapshot 模組模式 (2026-04-06)、_AIDocs 審計 M1-M10 (2026-04-06)、H1-H7 高優先修正 (2026-04-06)、TracePromptBreakdown (2026-04-06)、Sprint 8 (2026-04-05)、三份模組更新 (2026-04-05)、Memory Staleness (2026-04-05)、Task UI (2026-04-05)、prompt-assembler (2026-04-05)、/commit+/pr skills (2026-04-05)、CLI Provider (2026-04-05) -->
